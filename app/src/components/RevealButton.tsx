const RevealButton = () => {
  return (
    <button className="bg-primus-orange text-white py-2 w-20 h-10 transition-all cursor-pointer hover:text-white hover:bg-primary-h rounded-3px text-sm">
      Reveal
    </button>
  );
};

export default RevealButton;
